// Typical Implementation of Context
/// Context.js
// Initialize context API
import { createContext } from "react";

// Takes initial state to provide as argument (null)
const Context = createContext(null);

export default Context;
